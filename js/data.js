alert("data.js OK");

const boards = [
  { id: "arrivals", name: "TEST ARRIVALS" }
];

const threads = {
  arrivals: [
    { user:"test", time:"now", text:"test message" }
  ]
};

const users = {
  test: {
    status: "online",
    bio: "test user"
  }
};
